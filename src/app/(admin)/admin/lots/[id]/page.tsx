import { roboto } from '@/app/page';
import { Container } from '@/components/atoms/Container/Container';

export default async function LotsCrudPage() {
  return (
    <Container maxWidth="lg" className={roboto.className}>
      Crear
    </Container>
  );
}
